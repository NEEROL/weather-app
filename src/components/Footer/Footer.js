import React from 'react';
import styled from 'styled-components';

const StyledFooter = styled.footer`
    text-align: center;
`;

const Footer = () => {
    return <StyledFooter>Aplikacja pogody 😎 es</StyledFooter>;
};

export default Footer;
